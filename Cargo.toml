[package]
name = "stateful_telegram_assistant"
version = "0.1.0"
edition = "2024"

[dependencies]
teloxide = {version = "0.17.0", features = ["webhooks"]}
tokio = {version = "1.52.3", features = ["rt-multi-thread", "macros"]}
